// powinno być lepiej nazwane
import styled from 'styled-components';

const TileUnderHeader = styled.div`
    text-transform: uppercase;
    font-size: 20px;
    color: #00ADFF;
`;

export default TileUnderHeader;
